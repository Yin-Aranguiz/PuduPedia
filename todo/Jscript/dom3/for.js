let n = 0; // Inicializa una variable n con el valor 0

for (var i = 0; i < 9; i++) { // Un bucle que se ejecuta 9 veces, de i = 0 a i = 8
    n += i; // Suma el valor de i a n en cada iteración
    mifuncion(n); // Llama a una función llamada mifuncion y le pasa n como argumento
}

// Ejemplos de llamadas a una función i (parece que esto es un error o un malentendido)
// i(2)
// i(3)
// i(12)
    
